package nl.hva.miw.oop2.c12.hertentamen.Taxateur;

import java.util.Collection;
import java.util.HashSet;

/**
 * Een taxateur kan de totale waarde van een aantal dingen inschatten en
 * een totale waarde afgeven.
 * 
 * Deze taxateur gebruikt geen lijst maar een algemener type "Collection". 
 * In dit geval een HashSet. Lees de java API documentatie als je meer van
 * deze types wilt weten.
 * 
 * @author michel
 *
 */
public class Taxateur {
	
	Collection<Sieraad> taxatieSpullen = new HashSet<>();
	
	/**
	 * Voeg een sieraad toe aan de taxatieSpullen.
	 * 
	 * @param s
	 * @throws NegatieveWaarde 
	 */
	public void voegToe( Sieraad s ) {
		// Implementeer deze methode
	}
	
	
	/**
	 * Taxeert alle spullen van deze taxateur en geeft de totale waarde van deze spullen.
	 * 
	 * De totale waarde is de som van de waardes van de individuele spullen.
	 * De waarde van de individuele spullen wordt opgevraagd door de aanroep van "getWaarde()".
	 *  
	 * @return
	 */
	public double taxeerTotaleWaarde() {
		// Implementeer deze methode
		return 0;
	}
}
